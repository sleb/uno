{
  "name": "uno",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test:e2e": "firebase emulators:exec 'bun run playwright test'",
    "test:e2e:ui": "firebase emulators:exec 'bun run playwright test --ui'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@playwright/test": "^1.58.1"
  }
}
